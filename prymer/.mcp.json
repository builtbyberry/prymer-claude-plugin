{
    "mcpServers": {
        "prymer": {
            "type": "stdio",
            "command": "node",
            "args": [
                "${CLAUDE_PLUGIN_ROOT}/runtime/edge-broker.mjs",
                "--cloud-endpoint",
                "https://prymer.app/mcp/prymer"
            ]
        }
    }
}
